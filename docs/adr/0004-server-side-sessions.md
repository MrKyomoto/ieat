# 使用服务端会话而非 JWT

React 前端与 Go API 作为同域应用运行，邮箱和密码登录成功后由服务端创建随机会话，并通过具备 `HttpOnly` 和适当 `SameSite` 属性的 Cookie 传递会话凭据；生产 HTTPS 环境必须启用 `Secure`，本地 HTTP 开发环境关闭该属性。会话状态保存在 PostgreSQL，便于在退出登录、账号停用或角色变更时立即撤销权限，并配合同源限制和 CSRF 防护保护写操作。首版不使用 JWT，因为系统没有跨域客户端或独立服务之间传递自包含令牌的需求。
