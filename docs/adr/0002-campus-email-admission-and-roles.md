# 采用校园邮箱准入并将身份与角色分离

系统整体需要登录后访问。校内试运行时允许 `@mail.ustc.edu.cn` 和 `@ustc.edu.cn` 邮箱通过验证注册，并使用邮箱和密码登录；邮箱后缀只决定准入资格，账号默认均为普通用户，管理部门人员和平台管理员由平台显式授权。系统以不可变的内部用户标识关联评价等业务数据，校园邮箱失效后保留账号，未来统一身份认证作为新的登录身份与原账号绑定。开发阶段关闭注册和邮件发送，仅通过开发环境的数据初始化命令创建固定角色账号，且该初始化能力不得在生产环境启用。
